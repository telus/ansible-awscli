---
# host to test against
- hosts: test-kitchen
  remote_user: root

  roles:
    # name of role to test
    - role: ansible-aws-cli
      aws_cli_access_key: REDACTED
      aws_cli_secret_key: REDACTED
